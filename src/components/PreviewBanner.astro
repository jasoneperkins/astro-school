---
const isPreview = import.meta.env.PUBLIC_SANITY_VISUAL_EDITING === 'true'
---

{
	isPreview && (
		<div class="fixed right-0 bottom-0 left-0 z-50 flex items-center justify-between border-t border-amber-600 bg-amber-500 px-4 py-2 text-white shadow-lg">
			<div class="flex items-center gap-2 text-sm font-medium">
				<span class="relative flex h-3 w-3">
					<span class="absolute inline-flex h-full w-full animate-ping rounded-full bg-amber-100 opacity-75" />
					<span class="relative inline-flex h-3 w-3 rounded-full bg-amber-200" />
				</span>
				Preview Mode Active — Viewing Draft Content
			</div>
			<a
				href="https://your-studio-url.sanity.studio"
				target="_blank"
				class="rounded bg-white/20 px-3 py-1 text-xs font-bold tracking-wider uppercase transition-colors hover:bg-white/30">
				Open Studio
			</a>
		</div>
	)
}
